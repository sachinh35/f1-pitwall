server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router client-side routes - fall back to index.html for anything
    # that isn't a real static asset, instead of nginx 404-ing.
    location / {
        try_files $uri /index.html;
    }
}
